# Full Training Configuration
# Large-scale training (10M iterations, 8M infoset capacity).

resolver:
  max_raises_per_street: 4

solver:
  iteration_weighting: none

training:
  num_iterations: 10000000
  checkpoint_frequency: 10000
  iterations_per_worker: 500

card_abstraction:
  config: production

storage:
  initial_capacity: 8000000

system:
  config_name: "full_training"
