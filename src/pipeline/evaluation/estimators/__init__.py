"""The four things that put a number on a blueprint.

Filed together because they are peers -- a caller picks exactly one and the
choice is the evaluation's identity, recorded on every row. They had drifted
apart on the page: ``lbr`` was a package while ``public_tree_br`` -- the
primary, zero-variance estimator and the largest module here -- sat loose in
the parent beside the ledger and the reference oracles, so which files were
estimators was not answerable from the directory.

``reference/`` is deliberately NOT here. Those are textbook implementations
used to validate these; an oracle is not an estimator.
"""
